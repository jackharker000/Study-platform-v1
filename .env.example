TURSO_URL="libsql://study-questions-jackharker000.aws-ap-northeast-1.turso.io"
TURSO_TOKEN="<read-write JWT — server-side only, never expose to client>"
TURSO_READ_TOKEN="<read-only JWT — acceptable to expose if needed>"
# OPENAI_API_KEY not required — MCQ is graded automatically, structured questions are self-assessed via mark scheme
