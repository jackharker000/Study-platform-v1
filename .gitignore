.next/
node_modules/
*.db
*.db-journal
.env
.env.local
prisma/dev.db
prisma/*.db
dist/
.DS_Store
.vercel/cache

# Past-paper database (large, local-only)
database/

# Migration credentials — NEVER commit this
scripts/.env.migrate

# Migration progress (local state)
scripts/migration-progress.json
